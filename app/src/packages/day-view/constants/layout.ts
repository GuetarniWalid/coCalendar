import { DAY_TASKS_PROGRESS_SIZE } from '@project/day-tasks-progress';
import { DATE_SELECTOR_CELL_HEIGHT } from '@project/date-selector';

export const HEADER_ROW_PADDING_BOTTOM = 8;

export const HEADER_HEIGHT =
  DAY_TASKS_PROGRESS_SIZE +
  HEADER_ROW_PADDING_BOTTOM +
  DATE_SELECTOR_CELL_HEIGHT;
