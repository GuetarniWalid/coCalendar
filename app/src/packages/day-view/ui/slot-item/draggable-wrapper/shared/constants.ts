// Vertical constraint settings for drag behavior
export const VERTICAL_CONSTRAINT_DISTANCE = 50; // pixels the slot can move up or down when constrained

// Screen zone thresholds for auto-scrolling
export const VERTICAL_SCROLL_ZONE_TOP_THRESHOLD = 0.15; // 15% from top
export const VERTICAL_SCROLL_ZONE_BOTTOM_THRESHOLD = 0.85; // 85% from top

// Gesture settings
export const LONG_PRESS_DURATION = 500; // ms to activate drag
export const TAP_MAX_DURATION = 200; // ms for tap gesture

// Time handler dimensions
export const TIME_HANDLER_WIDTH = 80;
export const TIME_HANDLER_HEIGHT = 120;

// Animation durations
export const TIME_HANDLER_ANIMATION_DURATION = 200; // ms for appearance/disappearance animation

